╔══════════════════════════════════════════╗
║   STOCK MARKET PREDICTION PROJECT       ║
╚══════════════════════════════════════════╝

FOLDER STRUCTURE:
  stock_project/
  ├── app.py                  ← Python Flask + ML code
  ├── requirements.txt        ← Install list
  ├── README.txt              ← This file
  └── templates/
      └── index.html          ← Website (HTML frontend)

STEPS TO RUN:

1. Install libraries:
   pip install -r requirements.txt

2. Run the app:
   python app.py

3. Open browser:
   http://localhost:5000

That's it! Website + ML prediction both work together.
